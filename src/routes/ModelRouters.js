import Input from '../components/pages/SearchInput';
import Model from '../components/pages/model/model'; 

const routes = [
    {'path': '/', 'component': Input},
    {'path': '/model', 'component': Model},
];

export default routes